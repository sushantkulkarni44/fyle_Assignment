import { FilterTwoPipe } from './filter-two.pipe';

describe('FilterTwoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTwoPipe();
    expect(pipe).toBeTruthy();
  });
});
